import { PagePlaceholder } from '@/shared/ui'

export function OperatorsPage() {
  return (
    <PagePlaceholder
      title="Operadores"
      description="Alta de usuarios operadores por compañía (Fase C)."
    />
  )
}
